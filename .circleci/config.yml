version: 2.1
jobs:
  build:
    docker:
      - image: alpine:3.15

    steps:
      - run:
          name: Installing dependencies
          command: |
            cd frontend
            ls
            npm install
            npm run start